VITE_DIRECTUS_URL=https://crm.merkazneshama.co.il
VITE_DIRECTUS_TOKEN=your_directus_api_token_here

# Mobile-smoke (Playwright) — placeholders only; set via shell env, NOT this file.
# .env is bundled into the Vite build; do not put smoke creds here in real use.
# CRM_SMOKE_BASE_URL=http://127.0.0.1:5173
# CRM_SMOKE_EMAIL=test-account@example.com
# CRM_SMOKE_PASSWORD=replace_in_shell_env_not_in_file
