{
  "_comment": "Deployment allowlist for secrets-materialize. Install to /var/lib/crm-secrets/deploy-targets.json as root:root 0600. NEVER put a secret VALUE in this file or in Git - it holds NAMES and destinations only. 'secret' is the Secrets Registry entry NAME (never a value); 'env_var' is what the consumer reads. Replace every <PLACEHOLDER> before use.",
  "version": 1,
  "targets": [
    {
      "id": "deepgram-stt-bench",
      "secret": "<REGISTRY-SECRET-NAME>",
      "consumer": "hebrew-stt-bench",
      "path": "/etc/ai-secrets/deepgram.env",
      "env_var": "DEEPGRAM_API_KEY",
      "owner": "root",
      "group": "aisecrets",
      "mode": "0640"
    }
  ]
}
